rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // サーバーサイド（firebase-admin SDK）からのみアクセス
    // クライアントSDKからの直接アクセスは全て拒否
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
